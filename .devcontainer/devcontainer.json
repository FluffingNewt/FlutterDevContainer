{
	"name": "FlutterAppContainer",
	"dockerFile": "Dockerfile",

	"postCreateCommand": "sudo chown -R vscode /home/vscode/sdk/flutter/",

	"customizations": {
		"vscode": {
			"extensions": [
				"dart-code.dart-code",
				"dart-code.flutter"
			]
		}
	}
}
