{
	"name": "Flutter_DevContainer",
	"dockerFile": "Dockerfile",

	//"postCreateCommand": "sudo chown -R vscode /home/$USER/sdk/flutter/",

	"customizations": {
		"vscode": {
			"extensions": [
				"dart-code.dart-code",
				"dart-code.flutter",
				"github.vscode-github-actions"
			]
		}
	}
}
